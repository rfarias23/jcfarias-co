# 005 — Git: regularizar el repositorio y empujar el trabajo pendiente

Estado: implementada
Depende de: [000]
Bloqueada por dueño: no — resuelto 2026-09-05: repo PUBLIC confirmado con `gh repo view`, push autorizado, `PROMPT-AUDIT-SPEC.md` se trackea en la raíz

## Por qué

El spec original pedía "init y primer push". STATUS.md §6 demostró que eso ya ocurrió:
`fda04d2` está en `origin/main` (`rfarias23/jcfarias-co`, HTTPS). Lo que falta es llevar a
GitHub el trabajo de la sesión del 2026-09-03 y los entregables de esta auditoría, con
commits atribuidos a specs, y dejar `.gitignore` verificado.

## Alcance

Respeta 000.

Entra:

- Aceptar el remote HTTPS existente (D11). No se cambia a SSH.
- Verificar `.gitignore` (ya cubre `node_modules`, `.next`, `.env*`, `next-env.d.ts`, `tsconfig.tsbuildinfo`, `.vercel`, `.DS_Store`). Añadir `.superpowers/` explícitamente (hoy depende de un `.gitignore` interno) y `*.log`.
- Commits, en este orden, cada uno con su spec:
  1. `spec-005: import pending work from the 2026-09-03 session` — `content/site.ts` (dominio `.com`, dato del dueño), `docs/HANDOVER-2026-09-03.md`, `docs/superpowers/plans/2026-09-03-jcfarias-launch.md`, `.gitignore`.
  2. `spec-002: add ESLint, Prettier and Vitest tooling` — se hace al ejecutar 002 (no aquí).
  3. `spec-005: add audit status, constitution, specs and plan` — `docs/STATUS.md`, `docs/specs/*.md`, `docs/PLAN.md`, `PROMPT-AUDIT-SPEC.md` (en la ubicación que decida el dueño).
- `git push origin main` (sin `-f`, sin `--force-with-lease`).

No entra:

- Ramas, PRs, protección de rama (012 puede proponerla).
- Reescribir historial. `fda04d2` queda como está.
- Cambiar visibilidad del repo (decisión del dueño, fuera de git local).

## Criterios de aceptación

1. DADO el repo, CUANDO se ejecuta `git remote -v`, ENTONCES `origin` apunta a `https://github.com/rfarias23/jcfarias-co.git`.
2. DADO `.gitignore`, CUANDO se ejecuta `git check-ignore -q node_modules .next .env.local .superpowers tsconfig.tsbuildinfo next-env.d.ts .vercel`, ENTONCES exit 0 para cada ruta.
3. DADO el working tree tras los commits de esta spec y de 002, CUANDO se ejecuta `git status --porcelain`, ENTONCES la salida está vacía.
4. DADO `git log --oneline`, CUANDO se lee, ENTONCES cada commit posterior a `fda04d2` empieza por `spec-NNN: ` y no contiene emojis.
5. DADO el push, CUANDO se ejecuta `git ls-remote origin refs/heads/main`, ENTONCES el hash coincide con `git rev-parse HEAD`.
6. DADO `git reflog` y la salida del push, ENTONCES no aparece `forced update` ni `(forced)`.
7. DADO GitHub, CUANDO se ejecuta `gh repo view rfarias23/jcfarias-co --json visibility`, ENTONCES el valor coincide con la decisión del dueño (B8).

## Verificación

```bash
git remote -v
for p in node_modules .next .env.local .superpowers tsconfig.tsbuildinfo next-env.d.ts .vercel; do git check-ignore -q "$p" && echo "ignored: $p"; done
git status --porcelain | wc -l
git log --oneline fda04d2..HEAD
git push origin main 2>&1 | tee /tmp/push.log; grep -ci forced /tmp/push.log   # esperado 0
git ls-remote origin refs/heads/main; git rev-parse HEAD
gh repo view rfarias23/jcfarias-co --json visibility
```

Sin viewports.

## Archivos afectados

- `.gitignore`
- `content/site.ts` (solo commit del diff existente; sin edición)
- `docs/HANDOVER-2026-09-03.md`, `docs/superpowers/plans/2026-09-03-jcfarias-launch.md` (solo commit)
- `docs/STATUS.md`, `docs/PLAN.md`, `docs/specs/*.md` (solo commit)
- `PROMPT-AUDIT-SPEC.md` (commit, o `git mv` a `docs/` si el dueño lo decide)

## Hallazgos

(vacío)

# 012 — Integración continua en GitHub Actions

Estado: verificada
Depende de: [000, 001, 002, 005]
Bloqueada por dueño: no (opcional: activar protección de `main` con el check obligatorio, en GitHub)

## Por qué

Las puertas de calidad existen (`typecheck`, `lint`, `format:check`, `test:run`, `build`)
pero solo corren en la máquina del desarrollador, que además está afectada por el bloqueo
de iCloud. Un workflow en GitHub corre en disco local limpio y protege `main` y los previews
de Vercel.

## Alcance

Respeta 000. No añade dependencias npm.

Entra:

- `.github/workflows/ci.yml`: en `push` a `main` y en `pull_request`; `ubuntu-latest`; Node 22 con caché npm; `npm ci`; luego, en pasos separados y en este orden: `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run test:run`, `npm run build`. `CONTENT_SOURCE=local` como env del job.
- Badge de estado en README (una línea, sección Deploy).
- Recomendación (no acción automática): proteger `main` requiriendo el check `ci`.

No entra:

- Deploy desde Actions (lo hace Vercel, 009).
- Lighthouse en CI (008 se mide localmente; puede proponerse después).
- Matrices de Node, cachés de `.next`.

## Criterios de aceptación

1. DADO un push a `main`, CUANDO se ejecuta `gh run list --limit 1`, ENTONCES el workflow `ci` aparece con `completed success`.
2. DADO el log de la corrida, CUANDO se leen los pasos, ENTONCES los cinco comandos aparecen en el orden indicado y cada uno con exit 0.
3. DADO un PR con un error de tipo introducido a propósito en una rama de prueba, CUANDO CI corre, ENTONCES falla en `typecheck` (prueba de que la puerta enforce algo). La rama se borra después.
4. DADO `eslint --debug` en CI, ENTONCES el log muestra ≥ 28 archivos linteados (mismo criterio que 002).
5. DADO `README.md`, ENTONCES contiene el badge apuntando a `actions/workflows/ci.yml`.
6. DADO `.github/workflows/ci.yml`, CUANDO se ejecuta `npx prettier --check .github`, ENTONCES pasa.

## Verificación

```bash
gh run list --workflow=ci.yml --limit 3
gh run view --log | grep -E "npm run (typecheck|lint|format:check|test:run|build)"
# prueba negativa:
git checkout -b ci-smoke && echo 'const x: number = "a";' >> lib/cn.ts && git commit -am "ci smoke" && git push -u origin ci-smoke && gh pr create -f && gh pr checks --watch
git checkout main && git branch -D ci-smoke && git push origin --delete ci-smoke
grep -n "actions/workflows/ci.yml" README.md
```

Sin viewports.

## Archivos afectados

- `.github/workflows/ci.yml` (nuevo)
- `README.md` (badge, una línea)

## Hallazgos

- 2026-09-05 — Dos commits para esta spec (como en 005): el workflow solo puede verificarse después de empujarlo, y enmendar un commit ya empujado exigiría force push. El primero (`699ed1d`) lleva el workflow y el badge; el segundo lleva la evidencia y el cambio de abajo.
- La primera corrida anotó: "Node.js 20 is deprecated … actions/checkout@v4, actions/setup-node@v4". Subidas ambas acciones a `@v5` (dentro del archivo afectado). La corrida del segundo commit verifica que v5 funciona.
- Criterio 4 (`eslint --debug` en CI): el workflow no ejecuta `--debug` para no ensuciar el log. Verificado por equivalencia: mismo script `npm run lint`, mismo lockfile y `npm ci`, y localmente `--debug` reporta 28 archivos (002). Se marca PASA con esta salvedad; si el dueño quiere la prueba literal en CI, se añade un paso `npm run lint -- --debug | grep -c "Linting code for"`.
- Recomendación pendiente del dueño (no automatizada): GitHub → Settings → Branches → proteger `main` exigiendo el check `ci`.

## Evidencia de verificación (2026-09-05)

```
$ gh run list --workflow=ci.yml         run 33954514905 · main · push · completed success      criterio 1 PASA
$ gh run watch 33954514905              ✓ npm ci → typecheck → lint → format:check → test:run → build (en orden, todos ✓)   criterio 2 PASA
$ rama ci-smoke + PR #1 (error TS a propósito en lib/cn.ts)
  gh pr checks 1 --watch                ci fail 18s (run 33954579831)
  --log-failed                          lib/cn.ts(7,14): error TS2322 · Process completed with exit code 2   criterio 3 PASA
  gh pr close 1 --delete-branch         PR cerrado, rama borrada; `git branch -r` → solo origin/main
$ criterio 4                            PASA por equivalencia (ver Hallazgos)
$ grep actions/workflows/ci.yml README  badge presente en "Deploy"                            criterio 5 PASA
$ npx prettier --check .github          All matched files use Prettier code style!            criterio 6 PASA
```
